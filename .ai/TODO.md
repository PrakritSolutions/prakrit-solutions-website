# TODO

- Last Updated: 2026-09-26
- Generated By: Claude Code (Sonnet 5)
- Repository: git@github.com:PrakritSolutions/prakrit-solutions-website.git
- Branch: preview
- Documentation Status: Reconstructed from repository evidence

## NOW
- Back up out-of-repo state before the Mac reset (list in `CURRENT_STATE.md` > Restoration risk). Include `~/.claude/projects/-Volumes-SanDisk-Dual-128GB-Prakrit-Solutions/`.
- Owner to decide on committing `docs/` (untracked; only copy of legal drafts and tool scope docs). Suggested: commit on `preview`. This audit did not commit.
- Owner to decide on committing `.ai/`.

## NEXT
- Confirm production status: check Vercel Production env `COMING_SOON`, and which branch Production builds from. Relevant: `src/proxy.ts`, `src/lib/site-config.ts`, commit d10f3b6 on `main`.
- Launch: unset `COMING_SOON` on Production, ensure Production has `RESEND_API_KEY`, `NEXT_PUBLIC_GA_MEASUREMENT_ID` (production GA4 property), `SHEETS_*`, and that `NEXT_PUBLIC_SITE_URL` is unset there. Merge `preview` into `main` per owner's process. Legal effective date is 1 October 2026 (`legal-config.ts`), so time launch accordingly.
- Verify Resend domain sending for `noreply@prakritsolutions.in` in Production and a real end-to-end enquiry (email to hello@, confirmation email, Sheet row with Source).
- Run `upgrade()` in Apps Script if the live Sheet predates the Source/Fit columns, then re-deploy a new version (`integrations/google-sheets/README.md`).

## BLOCKED
- Naming Dealerwerx publicly: needs client written approval (owner memory rule). Relevant: `src/lib/content/case-studies.ts` slug `dealerwerx`.

## LATER
- Fill LinkedIn / X URLs in `socialLinks` (`site-config.ts`) once profiles exist.
- Consider durable rate limiting for `/api/contact` (current limiter is per-instance memory).
- Add tests (none exist), e.g. for `describeSource`, validation and the Sheet formula guard.
- Delete Sheet rows older than 24 months (stated in Privacy Policy).
- Add Node version pin (`engines` or `.nvmrc`) and CI (lint + tsc).

## IDEAS
- Free tools: SQL Query Generator, Invoice Extractor (see `docs/tools/`). Needs Anthropic key, Upstash Redis, Turnstile; not started.
- Marketing Command Center admin artifact (requested 2026-09-25; state UNKNOWN).

## NEEDS CONFIRMATION
- Was Privacy/Terms reviewed by counsel? Published pages exist; `docs/legal/*.md` drafts still say "pending legal review".
- Is `origin/preview` in sync (run `git fetch`)?
- Is the MyO case study approved for publication, and BookCargo App Store links current?
