# AI Instructions

- Last Updated: 2026-09-26
- Generated By: Claude Code (Sonnet 5)
- Repository: git@github.com:PrakritSolutions/prakrit-solutions-website.git
- Branch: preview
- Documentation Status: Reconstructed from repository evidence

## Before Starting Work
1. Read `AI_PROJECT_MEMORY.md`.
2. Read `CURRENT_STATE.md`.
3. Read `PROJECT_ARCHITECTURE.md` and relevant `DECISIONS.md` entries.
4. Read `AGENTS.md`/`CLAUDE.md` in the repo root: **this Next.js (16.3.4) has breaking changes; read `node_modules/next/dist/docs/` before writing Next code.**
5. Inspect the relevant source; follow existing patterns.

## Development Rules (observed)
- Language: TypeScript, path alias `@/` for `src/`. Server-only env vars are unprefixed; `NEXT_PUBLIC_` only where the client needs it.
- Content lives in `src/lib/content/*`, not inside components. Site-wide facts (emails, phone, URLs) live in `src/lib/site-config.ts`; legal facts in `src/lib/legal-config.ts`. Do not hardcode them elsewhere.
- Components: `src/components/{ui,sections,layout,analytics,forms,legal}`; pages are thin.
- Optional integrations (Resend, Sheets, GA) must degrade silently when env vars are unset. Keep that.
- Contact route: escape all visitor text in HTML (`escapeHtml`); never let secondary channels (Sheet, confirmation email) fail the request.
- Sheet cells must go through `safeCell` (formula injection). Test with `=1+1`.
- Never run Apps Script `setup()` on a Sheet with data; use `upgrade()`.
- Commit style: short imperative sentence, sentence case, no prefix (e.g. "Record which link each enquiry came from"). Attribution trailer per harness setting.
- Copy voice: human, polite, professional, enthusiastic via specifics; no hype, false urgency, unverifiable claims.
- Testing: none configured. Run `npx tsc --noEmit` and `npm run lint`.
- Package manager: npm.

## Change Discipline
- No unrelated refactors; preserve behaviour; inspect callers before changing interfaces.
- Do not delete code without evidence it is unused.
- Ask before architectural changes, merging `preview` into `main`, pushing, changing Vercel env, or sending anything externally.
- Do not commit `.env*`. Do not paste secrets into docs or chat.
- Never publish client names, metrics or outcomes the owner has not confirmed.
- Outbound marketing: draft only; the owner approves and sends.
- Browser work only in the Chrome profile "Prakrit Solutions" (verify signed-in account first).

## Context Preservation
Update when appropriate: `CURRENT_STATE.md`, `TODO.md`, `KNOWN_ISSUES.md`, `DECISIONS.md`, `SESSION_LOG.md`. Add a SESSION_LOG entry at the end of each meaningful session.
