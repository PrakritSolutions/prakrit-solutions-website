# Current State

- Last Updated: 2026-09-26
- Generated By: Claude Code (Sonnet 5)
- Repository: git@github.com:PrakritSolutions/prakrit-solutions-website.git
- Branch: preview
- Documentation Status: Reconstructed from repository evidence

Audit performed 2026-09-26 (project time zone IST).

## Last Updated
2026-09-26

## Current Phase
Pre-launch / launch preparation plus marketing start. Site feature-complete for a first version.

## Current Objective
Go live and acquire clients (30-day organic plan). Production go-live status NEEDS_CONFIRMATION.

## Current Work
Last commit: 94f7200 (2026-09-25) "Record which link each enquiry came from". Before that, lead-ops work (Sheet tracker, confirmation emails). Outside the repo, the marketing "Autopilot CMO" plan started 2026-09-25 (memory `project-autopilot-cmo`).

## Recently Completed
See CHANGELOG (2026-09). Latest: source attribution, `en_IN` locale, formula-injection fix in Sheet log, redesigned confirmation email, Geist fonts.

## In Progress
Launch/marketing (above). No half-finished code found: no TODO/FIXME markers in `src/` or `integrations/`.

## Blocked
Nothing demonstrably blocked. Launch depends on owner decisions (TODO "NEEDS CONFIRMATION").

## Known Problems
See KNOWN_ISSUES.md (ISSUE-001 to 006).

## Uncommitted Changes
- `git status`: only `?? docs/` (untracked, whole folder): `docs/legal/PRIVACY_POLICY.md`, `docs/legal/TERMS_OF_SERVICE.md`, `docs/tools/invoice-extractor-scope.md`, `docs/tools/sql-generator-scope.md`. Not staged, not committed by this audit.
- No modified tracked files. This audit adds `.ai/` (also untracked, not committed).
- Ignored local-only files: `.env.local`, `.next/`, `.vercel/`, `node_modules/`, `tsconfig.tsbuildinfo`, `next-env.d.ts`, `.DS_Store`.

## Recently Modified Files (mtime)
`package.json`, `package-lock.json`, `.env.example`, `integrations/`, `public/` Sep 24; `docs/` Sep 19 (legal) and Sep 14 (tools).

## Current Branch
`preview` (tracks origin/preview; origin/HEAD points to preview). `main` diverges: 1 commit ahead (d10f3b6, 2026-09-11, empty commit whose message records enabling COMING_SOON on production), 34 commits behind. Whether origin/preview is fully pushed: not checked with a fetch (NEEDS_CONFIRMATION; run `git fetch && git status -sb`).

## Recent Important Commits
94f7200 source attribution · 3f57eff Sheet formula guard · a4114fc Sheet tracker · 25630b4 visitor confirmation email · 4d10db5 Resend contact · 9ca83c2 cookie banner + legal pages · 3991dba apex canonical · 6364ed6 coming-soon gate.

## Current Build/Test State
- `npx tsc --noEmit`: passes (no output), 2026-09-26.
- `npx eslint`: no output (clean), 2026-09-26.
- `next build` not run (avoid writing `.next`; Vercel handles builds). Tests: none exist.
- Last `.next` build artifacts: Sep 25 11:58.

## Environment Requirements
Node + npm. Next 16.3.4 requires Node >=20.9.0 (verified in its package.json). Node v24.14.1 was installed at audit time. Env var names: `NEXT_PUBLIC_SITE_URL`, `COMING_SOON`, `RESEND_API_KEY`, `NEXT_PUBLIC_GA_MEASUREMENT_ID`, `SHEETS_WEBHOOK_URL`, `SHEETS_WEBHOOK_SECRET`, `SHEETS_URL`. Local `.env.local` currently contains only a Vercel-CLI-generated `VERCEL_OIDC_TOKEN` (regenerate with `vercel env pull`). Real values live in the Vercel dashboard (Production and Preview scopes) and Apps Script property `WEBHOOK_SECRET`.

## Pending Decisions
1. When to launch (turn off `COMING_SOON` on Production) and how `preview` reaches `main`/production.
2. Commit or discard `docs/` (recommend commit: it is the only copy of the tool scope docs).
3. Dealerwerx written approval before naming (memory rule) vs site currently naming it.
4. Legal review status of Privacy/Terms.
5. Build the two free tools or not.

## Immediate Next Step
Preserve state (backup list below), then resolve the production/launch question with the owner.

## Recommended First Action After Restoration
Answering "If I open this project tomorrow after the Mac is reset, what first?": clone the repo, `git checkout preview`, read `.ai/AI_PROJECT_MEMORY.md`, `npm install`, run `npm run dev`, and pull env with `vercel link && vercel env pull .env.local` (or restore `.env.local` from backup). Then verify `/`, `/contact` form (with Resend unset it only logs), and ask the owner about production status.

## Restoration risk (outside repo)
- `.env.local` and all Vercel env vars (RESEND_API_KEY, GA ID, SHEETS_*), Apps Script `WEBHOOK_SECRET` and the Sheet "Prakrit Enquiries" (real enquiry data).
- Claude conversation history and memory: `~/.claude/projects/-Volumes-SanDisk-Dual-128GB-Prakrit-Solutions/` (`*.jsonl` transcripts and `memory/` with 4 notes: voice rules, Chrome profile rule, private-repo access, Autopilot CMO plan).
- Private claude.ai artifacts (Client Acquisition Plan, Autopilot CMO Plan; links in memory).
- Vercel project config, domain DNS, Resend domain verification, Google Workspace, Calendly, GA4 property, GitHub org access, SSH key for `git@github.com`.
- `docs/` (untracked).
- Client source repos (GitLab BookCargo etc.) are outside this repo, private.

# POST-RESET RESTORATION CHECKLIST

1. Restore repository (`git clone git@github.com:PrakritSolutions/prakrit-solutions-website.git`, checkout `preview`; restore SSH key first).
2. Read `.ai/AI_PROJECT_MEMORY.md`.
3. Read `.ai/CURRENT_STATE.md`.
4. Restore required development tools (Node, npm, git, gh, Vercel CLI, Claude Code, Chrome with the "Prakrit Solutions" profile).
5. Restore environment variables/secrets separately (password manager / Vercel dashboard; never in git).
6. Install project dependencies (`npm ci`).
7. Verify build (`npm run build`).
8. Verify tests (none exist; run `npx tsc --noEmit` and `npm run lint`).
9. Verify external service configuration (Vercel envs per scope, Resend domain, Sheet webhook, GA4 property, Calendly link).
10. Review CURRENT_STATE.md.
11. Resume from "Immediate Next Step".
